---
name: "缠论结构分析"
description: "对指定股票在指定操作级别下的K线进行缠论走势结构分析（笔/中枢/买卖点），并自动生成带K线图、ZG/ZD标注的HTML结构报告。当用户输入『分析 {股票代码}，{操作级别}』（如『分析 600519，日线』）时自动执行，也可在用户要求做缠论结构/买卖点分析时调用。"
---

# 缠论结构分析

**一句话：** 输入「分析 {股票代码}，{操作级别}」（如 `分析 600519，日线`），自动拉取K线 → 拆笔/中枢/买卖点 → 生成带图的 HTML 结构报告。

> ⚠️ **使用前提**：需**通达信 MCP 数据源**（拉K线，技能本身不联网）+ **Python 3.10+**（本地计算，无第三方依赖）。首次使用按下面「快速上手」第 2 步保存数据。

---

## 快速上手（3 步）

1. **拉数据**：调通达信 `tdx_kline` 拉取该股该级别的 K 线（`tqFlag="1"` 前复权，`wantNum=300`）。
2. **存数据**：按约定格式存为 `assets/data/{code}_{period}.json`（格式见 `references/usage.md` 第 2 节）。
3. **跑脚本**：`python "assets/analyze.py" {code} {period} {名称}` → 生成 `assets/output/{code}_{period}/chan-{code}-{period}.html`，用 `present_files` 打开交付，并给出结构结论摘要。

**详细执行流程、级别映射、固定算法** → 见 `references/usage.md`（执行细节以该文件为准）。

---

## 使用实例（真实运行结果）

**输入**：`分析 300058，30分钟`（蓝色光标）

**输出摘要**（真实引擎输出）：
```
蓝色光标 300058 · 30分钟 · 27 笔 · 3 中枢
  中枢:
    [0] 笔0-10   ZG=13.94 ZD=13.42
    [1] 笔12-17  ZG=12.32 ZD=11.42
    [2] 笔19-26  ZG=16.02 ZD=15.23   ← 当前中枢
  当前: 现价 15.10  最近一笔 向下 16.98 → 14.72
```
**报告结论**：现价 15.10 运行于当前中枢（16.02–15.23）**下方**，最近一笔向下且已跌破中枢下沿 15.23，处于"跌破中枢下沿 · 紧盯反抽确认"状态。报告含 K 线图、笔/中枢标注、ZG/ZD 关键点位。

---

## 常见错误与处理

| 现象 | 原因 | 处理 |
|---|---|---|
| 脚本报「未找到数据文件」 | 数据没存到约定路径 | 按 `references/usage.md` 第 2 节重新保存到 `assets/data/{code}_{period}.json` |
| 脚本报「缺少 bars 数组或为空」 | 数据格式不对（如存成了 `kline` 字段） | 确认顶层是 `bars` 数组，字段为 `t/o/h/l/c` |
| 脚本报「bars[i] 字段缺失或数值非法」 | 单根K线缺字段或价格不是数字 | 检查该根数据的 t/o/h/l/c 是否齐全且可转数字 |
| 脚本报「high < low」 | 数据源价格异常 | 检查是否混入了脏数据，重新拉取 |
| 脚本报「不是合法 JSON」 | 文件被截断或损坏 | 重新拉取并完整保存 |
| 脚本输出「0 笔 · 无成笔」 | 极端行情（一字板）或数据过少 | 如实告知用户：当前行情无法成笔，建议切换更大级别分析 |
| 没有通达信 MCP | 连接器未配置 | 必须先提示「需要先连接通达信数据源」，**禁止编造数据** |
| K线不足 300 根 | 上市时间短或数据拉取不全 | 有多少用多少，数据不足时如实说明局限，不强行下结论 |

---

## 注意事项

- 分析严格基于固定算法，不引入主观预测；笔/中枢/ZG/ZD 为确定结果，**不得人工改动**。
- 操作建议仅供参考，不作为投资指令；涉及实际交易请结合风险管理。
- 结构调整（如成笔口径 MIN_GAP）只改 `assets/chanlon_engine.py` 顶部常量。
